# ledger

GitHub Pages를 프론트엔드, GitHub API를 백엔드로 활용하는 개인 도구.  
Personal Access Token을 프라이버시 레이어로 사용 — 퍼블릭 저장소지만 토큰 없이는 데이터 접근 불가.

---

## 구성 파일

| 파일 | 설명 |
|------|------|
| `index.html` | 가계부 앱 (메인) |
| `memo.html` | 메모 앱 (부가) |
| `data.nfo` | 가계부 데이터 (JSON) |
| `memo.nfo` | 메모 데이터 (JSON) |
| `access.nfo` | 출입비번 데이터 (JSON) |
| `raw/` | 메모 첨부 파일 (이미지·오디오·PDF) |
| `backups/` | 가계부 txt 백업 보관 폴더 |
| `.gitignore` | 로컬 유틸리티 동기화 제외 설정 |

---

## 가계부 (index.html) — 메인 기능

### 잔고 구조
- 슈잔고 / 바잔고 / 카잔고 / 지잔고 4개 계정으로 구성
- 카잔고는 세부항목(카뱅·농협·세이프·mmf·카Pay)으로 분리 관리
- 총잔고 및 계정별 잔고 실시간 표시
- 가용잔고 추가 표시: 총잔고 − 지잔고 + (슈잔고 − 300) + (바잔고 − 고정잔액 − 300)
  - 지갑 속 현금(지잔고)은 지출 할당 대상이 아니므로 제외
  - 슈잔고·바잔고의 이전수수료 예비분(각 300)은 가용 범위에서 빼고, 그 외 잔액은 가용으로 가산

### 거래 입력 (입력 탭)
- **수입**: 카테고리 태그 선택 → 총수입 + 수수료 입력 → 순수입 자동 계산
- **지출**: 카테고리 + 차감 계정 선택 → 금액 입력
- **잔고 이전**: 계정 간 자금 이동, 수수료 처리 포함. 카잔고 관련 시 세부항목 선택
- **잔액 보정**: 실제 잔액 입력 → 계산값과 차이를 자동 수입/지출 처리
- **바잔고 월정산**: 고정잔액 변경 시 감소분을 수수료(지출)로 자동 처리
- 오늘 내역 하단 실시간 표시

### 리뷰 탭
- 기간 필터로 거래 내역 조회
- GitHub 저장/불러오기 (☁)
- 로컬 txt 백업 다운로드 / backups/ 폴더에서 txt 가져오기 (병합)

### 설정 탭
- 수입·지출 카테고리 커스터마이징
- 잔고 계정 추가/삭제
- 카잔고 세부항목 관리
- GitHub 토큰 관리

### GitHub 동기화
- 자동 저장: 거래 입력 시 디바운스 후 GitHub에 자동 반영
- 탭 재활성화 시 자동 불러오기
- 페이지 이탈 전 미저장 데이터 강제 flush
- 데이터 형식: qcode 기반 JSON (`data.nfo`)

---

## 메모 (memo.html) — 부가 기능

### 기본 구조
- qcode 기반 식별자 (`연도w주차요일v시간10분블록`, 예: `26w271v194`)
- 탭: 작성 / 검색 / 출입비번
- 작성 탭에서 현재 주차의 메모 전체를 표시, "이전 주차 더보기"로 과거 주차 이어서 조회
- 검색: 일반 텍스트 검색 + qcode 패턴 자동 인식(`26w27`=주차 전체, `26w271`=요일까지)
- 검색 범위에 본문·qcode·첨부파일명(원본 파일명) 포함, 매칭 시 하이라이트
- 새로고침(F5) 시 마지막으로 보던 탭 유지
- 인라인 텍스트 편집
- 특수문자 삽입 (✶ ✦ ✧ ✬ ⛧)

### 첨부 파일
- **이미지**: 인라인 표시, 클릭 시 라이트박스. PNG → JPEG 자동 변환 (Canvas API, 91% 품질). 파일명 qcode 자동 부여. "교체" 버튼으로 기존 파일명을 유지한 채 보정본으로 덮어쓰기 가능 (교체 전 확인창 표시)
- **오디오**: 인라인 플레이어. 업로드 원본 파일명 표시. 파일명 ✏️ 버튼으로 수정 가능. GitHub API fetch → Blob URL 주입 방식으로 로드
- **PDF**: 아이콘 + 파일명 링크. 클릭 시 크롬 PDF 뷰어로 새 탭 오픈. 파일명 ✏️ 버튼으로 수정 가능
- 첨부 파일 개별 삭제 (✕)
- 저장 경로: `raw/{qcode}_{번호}.{확장자}`

### 출입비번 탭
- 장소별 출입비번 저장·조회 (가계부에서 이전됨)
- 장소명 인라인 수정 가능 (✏️ 버튼)
- 등록일을 qcode 기반 `26w271` 형식으로 표시
- 장소명 검색 지원
- 별도 `access.nfo` 파일로 저장

---

## 2026-06-29 개선 (메모 기능)

- **PDF 첨부 추가**: 이미지·오디오에 이어 PDF 첨부 지원. 아이콘+파일명 표시, 클릭 시 크롬 PDF 뷰어로 오픈
- **오디오 로드 방식 전환**: `raw.githubusercontent.com` 직접 src → GitHub API fetch → Blob URL 주입 방식으로 변경
- **대용량 파일 처리**: 1MB 이하는 Contents API, 초과 시 Git Blobs API로 자동 fallback (오디오·PDF 공통)
- **파일명 표시**: 오디오·PDF 모두 업로드 원본 파일명 표시 (`origName` 필드 저장)
- **파일명 인라인 편집**: 오디오·PDF 파일명 수정 가능, 즉시 저장
- **첨부 개별 삭제**: 이미지·오디오·PDF 각각 ✕ 버튼으로 개별 삭제 가능

## 2026-06-30 개선

### 출입비번 기능 이전
- 가계부(index.html)에 있던 출입비번 탭을 메모(memo.html)로 완전 이전
- 별도 `access.nfo` 파일로 분리 저장 (`migrate_access.py`로 data.nfo에서 분리, 원본은 `data0.nfo`로 백업)
- 기존 입력 UI(장소·비번 입력, 검색, 삭제) 그대로 계승
- index.html에서 출입비번 관련 탭·UI·함수·데이터 필드 전체 제거

### 메모 구조 전면 개편
- 타임라인 탭, 주차 탭 제거
- 최근 메모를 qcode 기준 "현재 주차 전체"로 표시, "이전 주차 더보기"로 확장 조회
- 검색에 qcode 패턴 자동 인식 추가
- 검색 범위에 첨부파일명 포함
- 새로고침(F5) 시 마지막 탭 유지 (localStorage)

### 오디오 재생 버그 수정
- 동일 메모가 여러 영역(최근 메모·검색 결과)에 동시 표시될 때 `<audio>` id 충돌로 일부 플레이어가 재생되지 않던 문제 해결 — 영역별 컨텍스트를 id에 반영해 충돌 제거
- Git Blobs API 응답 헤더 오류(`raw+json` → `+json`)로 1MB 초과 오디오/PDF 로드가 실패하던 문제 수정

### 이미지 교체 기능
- 메모 내 이미지 첨부에 "교체" 버튼 추가
- 폰 촬영 이미지를 데스크탑에서 보정한 파일로, 기존 저장 파일명을 유지한 채 덮어쓰기 가능
- 교체 전 확인창 표시

### 가계부 가용잔고 표시
- 총잔고 옆에 가용잔고 추가 표시
- 가용잔고 = 총잔고 − 지잔고 + (슈잔고 − 300) + (바잔고 − 고정잔액 − 300)

---

## 로컬 유틸리티

### cleanup_raw.py
`memo.nfo`와 `raw/` 폴더를 대조하여 미참조 파일을 `deleted/` 폴더로 격리.

```
python cleanup_raw.py
```

빌드:
```
pyinstaller --onefile --console cleanup_raw.py
```

### migrate_access.py
`data.nfo`에서 `access` 필드를 분리해 `access.nfo`로 저장하고, 원본은 `data0.nfo`로 백업. 1회성 마이그레이션 스크립트.

```
python migrate_access.py
```

### .gitignore 제외 항목
```
cleanup_raw.py
cleanup_raw.exe
migrate_access.py
deleted/
```

---

## 기술 메모

- `raw.githubusercontent.com`은 `Content-Type: application/octet-stream` 강제 → fetch 시 CORS 차단. API 경유 필수
- GitHub Contents API는 1MB 초과 파일에서 `content` 필드 비어 반환 → Git Blobs API로 대응
- Git Blobs API 호출 시 `Accept: application/vnd.github+json` 사용 (raw+json 사용 시 base64 파싱 실패)
- `window.open('', '_blank')`를 클릭 동기 컨텍스트에서 먼저 열고, fetch 완료 후 `tab.location.href` 교체해야 팝업 차단 없이 새 탭 오픈
- 같은 메모가 여러 화면 영역에 동시 렌더링될 경우, DOM id에 영역 구분자(ctx)를 포함하지 않으면 `getElementById`가 첫 번째 요소만 찾아 나머지가 비정상 동작함
- 카잔고 총액은 항상 세부항목 합으로 재계산 (단일 진실 소스)

---

## 로컬 동기화

- 저장소 경로: `E:\github\wiza0ard\ledger`
- 도구: GitHub Desktop
- Pull → 로컬 작업 → Commit → Push
- 데이터 파일(`data.nfo`, `memo.nfo`, `access.nfo`)은 앱이 직접 갱신하므로 GitHub Desktop에서 커밋 대상에서 제외하는 것을 권장 (코드 파일만 선택 커밋)
